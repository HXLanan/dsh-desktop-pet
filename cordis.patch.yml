# dsh-desktop-pet profile patch layer — insert one loader entry for the plugin
# package (host half). The browser half is picked up by dsh-client-modules
# through the package's dsh.client declaration.
- insert:
    - id: desktop-pet
      name: 'dsh-desktop-pet'
